# LeLamp 待办

## Todo List

- [ ] 尝试舵机噪声分类器，区分“只有舵机声”和“舵机运动时有人插话”。
- [ ] 开发小灯视觉功能。
- [ ] 开发“专注模式”
- [ ] 开发todo快速记事功能
- [x] 开发 health tool 系统一键自检（`uv run --no-sync -m lelamp.diagnostics`；物理效果仍需人工确认）
- [ ] 尝试开发lelamp用website控制端
- [ ] 解决TTS播报完之后的短暂延迟

## 详细执行方案

### 尝试舵机噪声分类器

#### 背景与当前结论

目标是在小灯播报并执行动作时可靠检测真人插话，避免舵机声音误触发 Barge-in。

已经完成的实验：

- WebRTC AEC 能压低扬声器回声，并能检测真人开口。
- AEC 检测开口后停止播放、切换 raw mic，可以保留后续语句。
- 普通 Silero VAD 会把舵机运动声判断为人声；把阈值从 `0.25` 提高到 `0.7` 仍会误触发，同时真人检测明显变慢。
- 已采集 54 段、18 类运动的双通道舵机声音。不同动作的稳定峰值分散在约 `0.5～7.9 kHz`，没有跨动作通用的固定窄带；固定 notch 前后均有 20 个误触发片段，因此固定频率滤波不可行。

已有数据位于 Pi5：

```text
~/lelamp_runtime/voice_debug/servo_noise/20260917-164636/
```

相关程序：

```text
lelamp/test/test_servo_noise_capture.py
lelamp/test/analyze_servo_noise.py
lelamp/test/test_dynamic_barge_in.py
```

#### 推荐方案

建立轻量二分类器，输入最近 `200～300 ms` 的 AEC clean 双通道音频，输出：

```text
servo_only
human_speech
```

不要部署大型模型。优先提取以下特征，并训练逻辑回归或小型树模型：

- 时间包络、RMS 变化和持续时间。
- 频谱质心、带宽、平坦度和滚降点。
- 谐波性、过零率、MFCC 或简化 Mel 频带能量。
- 相邻窗口的频谱变化速度。
- 双麦克风能量差、相关性和相位差。
- `current_motion_task`、动作类别和运动阶段等已知状态。

训练完成后只把模型系数或小型模型文件部署到 Pi5；实时推理继续使用 NumPy/ONNX Runtime，不能引入大型音频模型。

#### 数据采集

先做约 10 分钟快速可行性采集：

1. 安静环境和常见敲击、触碰、桌面噪声。
2. 只有真人说话：短词、短句、长句。
3. 选择 `nod、headshake、excited、底座转向、照明姿态切换`，采集只有舵机的声音。
4. 相同动作期间，按终端提示说：
   - “停”
   - “等等”
   - “行了”
   - “别说了”
   - 2～4 秒正常句子
   - 5～8 秒长句
5. 覆盖正常音量、轻声、快速说话，以及正面、略偏左、略偏右三个方向。

若快速实验有效，再补充约 10～15 分钟数据，覆盖全部动作、TTS＋舵机＋真人以及不同姿态和负载。

每段录音必须保存：

- 48 kHz 双通道原始麦克风。
- AEC clean 音频。
- 动作名称、开始/结束时间和是否正在播放 TTS。
- 真人说话开始/结束时间及原句文本。

#### 训练与验证

- 以 `200～300 ms` 滑动窗口生成样本，训练、验证和测试必须按整段录音划分，禁止把同一录音的相邻窗口分到不同集合。
- 使用“留出动作测试”：至少保留一种未参与训练的动作，检查是否能泛化到新动作。
- 比较单通道、双通道及加入运动状态后的效果，选择满足指标的最轻方案。
- 先离线回放验证，不接入正式 Barge-in；通过后再封装为 voice 基础组件，由 `LampApp` 提供运动状态。

#### 验收标准

- 舵机运动误触发率低于 `1%`。
- 真人插话检出率至少 `95%`。
- “停”“等等”等短词不能漏检。
- 插话检测延迟不超过 `250 ms`。
- Pi5 平均 CPU 增量低于约 `5%`，额外内存明显低于 `100 MB`。
- 留出的未知动作仍达到相同量级的准确率。

若快速采集后无法达到约 `90%` 的离线区分准确率，停止扩充数据，改用可靠的状态门控：舵机运动期间暂停自然 Barge-in，动作结束后恢复；不要继续堆叠复杂模型。

#### 架构边界

- 该功能属于语音基础设施，不是 Agent Tool。
- Agent、ASR、TTS、Motion 不直接互相控制；`LampApp` 继续负责 `speaking` 与 `current_motion_task` 状态协调。
- 不修改或覆盖用户动作 CSV、校准文件和姿态配置。
- 未达到验收标准前，不接入正式 `lelamp.app`。

### 开发小灯视觉功能

摄像头选型与候选链路验证已经完成。正式摄像头为固定在灯头上的IMX179 USB UVC摄像头，采用640×480@30 FPS MJPEG采集；固定安装画面转正后的模型输入为240×320。正式最新帧采集、联合感知、生命周期、健康状态和人脸双轴流式跟踪已经接入`LampApp`。

- [x] 建立最新帧采集、正式数据结构和VisionController生命周期。
- [x] 分离语音连续会话与持续模式基础生命周期，增加持续模式感知的重新唤醒入口。
- [x] YuNet和Gesture Recognizer使用同一最新帧并同时保持运行，默认最高10 Hz。
- [x] 为`ToolExecutor`调用补充可信来源，避免视觉动作被误判为Agent自主情绪动作。
- [x] 完成单人前景目标保持、背景小脸候选过滤和结果过期保护。
- [x] 完成人脸机械跟随所需的安装方向修正、双轴关节响应标定和Motion流式控制。
- 现场验收真实人物跟随并调节增益，随后增加肩/肘慢速姿态回中和无目标超时退出。
- 在同一tracking runner中增加手部跟随、稳定手势和face/hand目标切换。
- WORK_LIGHT长期保留手势感知，在其内部增加手部引导和自定义照明姿态恢复；临时动作期间丢弃视觉控制事件。
- SFace正式身份激活和按需物体检测等待明确开发指令。

详细方案统一维护在视觉目录：

- [视觉模块职责与当前状态](lelamp/vision/README.md)
- [视觉功能设计与开发准则](lelamp/vision/docs/VISION_DESIGN.md)
- [当前开发路线](lelamp/vision/docs/ROADMAP.md)
- [历史摄像头选型验证方案](lelamp/vision/docs/CAMERA_EVALUATION.md)
